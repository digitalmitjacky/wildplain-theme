<div class="section container page-content">
  <h1 class="section-title">{{ page.title }}</h1>
    <div class="page-body rte">{{ page.content }}</div>
    </div>
    <style>
    .page-content{max-width:800px}
    .page-body{color:#5A4E42;line-height:1.8;font-size:0.95rem}
    .page-body h2,.page-body h3{font-family:var(--font-display);font-weight:900;text-transform:uppercase;margin:24px 0 12px}
    .page-body p{margin-bottom:16px}
    .page-body a{color:var(--color-sand);text-decoration:underline}
    </style>
